import { TPositionConfig } from './types';

export const positionConfig: TPositionConfig = {
  center: {
    top: '50%',
    left: '50%',
    transform: 'translate(-50%, -50%)',
  },
  top: {
    top: '5%',
    left: '50%',
    transform: 'translate(-50%, 0)',
  },
};
