import { TColorConfig, TIconConfig } from './types';

export const colorConfig: TColorConfig = {
  primary: 'black',
  secondary: 'blue00',
};

export const iconConfig: TIconConfig = {
  primary: 'arrow-chevron-left',
  secondary: 'arrow-mini-left',
};
