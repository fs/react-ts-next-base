import { TColorConfig, TVariantConfig } from './types';

export const colorConfig: TColorConfig = {
  primary: 'blue00',
  secondary: 'blue',
  light: 'white',
};

export const fontConfig: TVariantConfig = {
  primary: {
    fontWeight: 700,
    size: '0.875rem',
  },
  secondary: {
    fontWeight: 600,
    size: '0.875rem',
  },
  light: {
    fontWeight: 200,
    size: '0.75rem',
  },
};
