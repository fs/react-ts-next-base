import styled from 'styled-components';
import theme from 'public/styles/theme';

export const Wrapper = styled.div`
  background: ${theme.colors.greyFO};
  padding: 1rem;
`;

export const LightWrapper = styled.div`
  padding: 1rem;
`;
