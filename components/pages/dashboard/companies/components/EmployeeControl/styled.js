import styled from 'styled-components';

export const EmployeeControlWrapper = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  row-gap: 1rem;

  > button {
    font-size: 0.75rem;
    font-weight: 400;
    padding: 0;
  }
`;
