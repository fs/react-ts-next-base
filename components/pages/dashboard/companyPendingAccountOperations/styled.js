import styled from 'styled-components';

export const Wrapper = styled.div`
  padding: 2rem;
`;

export const Subtitle = styled.h3`
  display: flex;
  align-items: center;
  column-gap: 0.5rem;
  font-size: 0.875rem;
  font-weight: normal;
  margin: 1.5rem 0 0;
`;
