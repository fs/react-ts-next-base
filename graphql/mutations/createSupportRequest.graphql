mutation createSupportRequest($input: SupportRequestInput!) {
  createSupportRequest(input: $input) {
    email
    id
    images {
      id
      imageUrl
    }
    message
    subject
    user {
      id
      email
    }
  }
}
