mutation updatePassword($input: UpdatePasswordInput!) {
  updatePassword(input: $input) {
    me {
      avatarUrl
      confirmedAt
      email
      firstName
      id
      lastName
    }
    accessToken
    refreshToken
  }
}
