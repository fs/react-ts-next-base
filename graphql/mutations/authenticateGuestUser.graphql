#import "../fragments/currentUserInfo.graphql"
#import "../fragments/authTokens.graphql"

mutation authenticateGuestUser {
  authenticateGuestUser {
    me {
      ...CurrentUserInfo
    }
    ...AuthTokens
  }
}
