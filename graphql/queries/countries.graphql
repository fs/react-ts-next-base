query Countries($after: String, $name: String) {
  countries(after: $after, name: $name) {
    edges {
      cursor
      node {
        id
        name
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
